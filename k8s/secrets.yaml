apiVersion: v1
kind: Secret
metadata:
  name: threatintel-secrets
  namespace: threat-intelligence
type: Opaque
data:
  # Base64 encoded values (examples - replace with actual)
  # echo -n "your-api-key" | base64
  NVD_API_KEY: ""  # Optional: NVD API key for higher rate limits
  DATABASE_PASSWORD: dGhyZWF0aW50ZWw=  # "threatintel" in base64
  REDIS_PASSWORD: ""
